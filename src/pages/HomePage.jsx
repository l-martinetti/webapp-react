import MovieCard from "../components/MovieCard"

const Homepage = () => {
    return (
        <div className="container pt-3 d-flex gap-3 flex-wrap">
            <MovieCard />
        </div>
    )
}

export default Homepage